# tests/analysis/test_job_images.py — REMOVED
#
# The Job Image Storage tools (save_job_image, list_job_images,
# delete_job_image, get_job_images_path, set_job_images_path) and their
# tests were removed from AI-Prowler. This file is now an empty stub —
# safe to delete from disk entirely; kept only so no stray import/collection
# error occurs if something still references the old path.
